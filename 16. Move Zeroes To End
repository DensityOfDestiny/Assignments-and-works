void pushZerosAtEnd(vector<int> &arr) {
  int i, j = 0;
  vector<int> a(arr.size(), 0);

  for (i = 0; i < arr.size(); i++) {
    if (arr[i] != 0) {
      a[j] = arr[i];
      ++j;
    }
  }

  arr = a;
}
