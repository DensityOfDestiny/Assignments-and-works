#include <bits/stdc++.h> 
vector<int> reverseArray(vector<int> &arr , int m)
{
	int n=arr.size();
    int i,temp;
	vector<int> arr1=arr;
	int k=n-1,j=m+1;
    int num=(n-m)/2;
	for(i=n-1,j=m+1;i>=m+1,j<=n-1;i--,j++){
        arr[i]=arr1[j];
	}		
	
        
	return arr;
}
