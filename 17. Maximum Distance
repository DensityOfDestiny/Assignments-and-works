#include <bits/stdc++.h>
int maxDiff(vector<int> &a, int n) {
  int max = -1;
  int i, j;

  for (i = 0; i < n-1; i++) {
    for (j = n - 1; j >= i + 1; j--) {
      if (a[i] <= a[j]) {
        break;
      }
    }
    if (j - i > max) {
      max = j - i;
    }
  }
  return max;
}
