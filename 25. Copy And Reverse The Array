#include <bits/stdc++.h>
vector<int> copyAndReverse(vector<int> arr, int n) {
  int i;
  vector<int> COPY_ARR;
  for (i = n - 1; i >= 0; i--) {
    COPY_ARR.push_back(arr[i]);
  }
  return COPY_ARR;
}
