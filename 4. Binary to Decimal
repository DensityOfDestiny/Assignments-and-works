#include <bits/stdc++.h>

int convert(int N, string &str) {
  int i;
  int val = 0;
  for (i = N - 1; i >= 0; i--) {
    if (str[i] == '1') {
      val = val + pow(2, N - i - 1);
    }
  }
  return val;
}
