#include <bits/stdc++.h>
int missingNumber(vector<int> &arr, int n) {
  int count = 0;
  int i;
  int sep = (arr[n - 1] - arr[0]) / n;

  for (i = 0; i < n; i++) {
    if (arr[i + 1] - arr[i] != sep) {
      return arr[i] + sep;
    }
  }
}
